/**
 * Bundled minimum app version for startup checks. Keep in sync with
 * `MIN_REQUIRED_VERSION` in `backend/trpc/routes/farm.ts` when raising the floor.
 */
export const BUNDLED_MIN_SUPPORTED_VERSION = '1.0.0';
